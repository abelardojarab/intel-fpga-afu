A simple AFU template that can be used as a starting point for writing AFUs.
The RTL satisfies the bare miminum requirement, responding to MMIO reads to
return the device feature header and the AFU's UUID.
